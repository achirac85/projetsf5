{% extends "admin/base.html.twig" %}

{% block body %}
    <table class="table table-bordered">
            <thead class="thead dark">
                <tr>
                    <th>Auteur</th>
                    <th>Titre</th>
                </tr>
            </thead>
                    
            <tbody>
            {% for livre in livres %}
                <tr>
                    <td>{{ livre.auteur }}</td>
                    <td>{{ livre.titre }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

{% endblock %}